<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Identify Plant - Green Care</title>
    <link rel="stylesheet" href="styles.css">
    <style>
        /* General Styles */
body {
    font-family: 'Arial', sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f4f7f6;
    color: #333;
}

/* Header */
header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 20px 10%;
    background-color: #2e7d32;
    color: white;
}

header .logo h1 {
    margin: 0;
    font-size: 2rem;
    font-family: 'Pacifico', cursive;
}

header nav ul {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
}

header nav ul a {
    margin-left: 20px;
}

header nav ul a {
    color: white;
    text-decoration: none;
    font-size: 1rem;
    transition: color 0.3s ease;
}

header nav ul a:hover {
    color: #a5d6a7;
}

/* Hero Section */
.identify-hero {
    background: url('/farmer.jpg') no-repeat center center/cover;
    height: 60vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    color: white;
    animation: fadeIn 2s ease-in-out;
}

.identify-hero h2 {
    font-size: 3rem;
    margin-bottom: 20px;
    animation: slideIn 1s ease-in-out;
}

.identify-hero p {
    font-size: 1.2rem;
    margin-bottom: 40px;
    animation: slideIn 1.5s ease-in-out;
}

/* Form Section */
.identify-form {
    padding: 50px 10%;
    text-align: center;
}

.identify-form h2 {
    font-size: 2rem;
    margin-bottom: 30px;
}

.image-label {
    display: block;
    font-size: 1.2rem;
    margin-bottom: 20px;
}

#image {
    padding: 10px;
    margin-top: 10px;
    font-size: 1rem;
}

.submit-button {
    padding: 15px 30px;
    font-size: 1.2rem;
    background-color: #66bb6a;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s ease;
    animation: fadeInUp 2s ease-in-out;
}

.submit-button:hover {
    background-color: #81c784;
}

.link-button {
    display: inline-block;
    margin-top: 20px;
    font-size: 1rem;
    color: #2e7d32;
    text-decoration: none;
    transition: color 0.3s ease;
}

.link-button:hover {
    color: #81c784;
}

/* Footer */
footer {
    background-color: #2e7d32;
    color: white;
    text-align: center;
    padding: 20px 0;
}

/* Animations */
@keyframes fadeIn {
    from { opacity: 0; }
    to { opacity: 1; }
}

@keyframes slideIn {
    from { transform: translateY(-20px); }
    to { transform: translateY(0); }
}

@keyframes fadeInUp {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}

    </style>
</head>

<body>
    <header>
        <div class="logo">
            <h1>Green Care</h1>
        </div>
        <nav>
            <ul>
                <a href="/">Home</a>
                <a href="/identify">Identify Plants</a>
                <a href="/health">Check Health</a>
                <a href="#">About</a>
                <a href="#">Contact</a>
            </ul>
        </nav>
    </header>

    <section class="identify-hero">
        <h2>Identify Your Plant's Health with Ease</h2>
        <p>Simply upload an image and let our advanced AI do the rest.</p>
    </section>

    <section class="identify-form">
        <h2>Upload a Plant Image</h2>
        <form action="/health" method="POST" enctype="multipart/form-data">
        <label for="image">Upload an Image:</label>
        <input type="file" name="image" id="image" required>
        <br>
        <button type="submit" class="submit-button">Plant Health Check</button>
    </form>
        
    </section>

    <footer>
        <p>&copy; 2024 Green Care. All rights reserved.</p>
    </footer>

    <script src="script.js"></script>
    <script>
        // Add any custom JavaScript for animations or interactivity here
document.addEventListener('DOMContentLoaded', function() {
    // Example: Smooth scroll to sections
    document.querySelectorAll('nav ul li a').forEach(anchor => {
        anchor.addEventListener('click', function(e) {
            e.preventDefault();
            document.querySelector(this.getAttribute('href')).scrollIntoView({
                behavior: 'smooth'
            });
        });
    });
});

    </script>
</body>

</html>
